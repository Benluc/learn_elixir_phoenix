<h1>List Users</h1>

<table>
  <%= for user <- @users do %>
    <tr>
      <td>
        <strong><%= first_name(user) %></strong> (<%= user.id %>)
      </td>
      <td>
        <%= link "View", to: Routes.user_path(@conn, :show, user.id) %>
      </td>
  <% end %>
</table>